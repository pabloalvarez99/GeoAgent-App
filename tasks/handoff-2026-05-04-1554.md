# Handoff — 2026-05-04 15:54

## Estado actual

Plan F0+F1 (`docs/superpowers/plans/2026-05-04-geology-super-cockpit.md`): **4/15 tasks completas**. Próxima: Task 5 (Firestore rules + indexes multi-org RBAC).

## Trabajo entregado en esta sesión

| Commit | Mensaje |
|---|---|
| `c441c6f` | feat(super): add recharts dep + org/reconc type definitions |
| `509214c` | chore(prompts): registrar prompt 15:01 execute-plan-task-1 |
| `3597f00` | feat(rbac): role permission matrix with tests covering 6 roles |
| `6a5e4a4` | chore(prompts): registrar prompt 15:05 execute-plan-task-2 |
| `352d6ab` | feat(reconc): factor computation + classification + outlier detection |
| `f9b2e45` | chore(prompts): registrar prompt 15:13 execute-plan-task-3 |
| `f35751b` | feat(qaqc): Shewhart bands + HARD duplicates + bias + blank classifiers |
| `819eb7f` | chore(prompts): registrar prompt 15:18 execute-plan-task-4 |

**Modo:** subagent-driven (un subagente por task con review checkpoint entre tasks).

## Tasks completas

- ✅ **Task 1** — recharts + `types/org.ts` + `types/reconc.ts`. Type-check clean.
- ✅ **Task 2** — RBAC matrix (`lib/auth/rbac.ts` + tests). 8/8 pass.
- ✅ **Task 3** — Reconciliación calc (`lib/data/reconc.ts` + tests). 13/13 pass.
- ✅ **Task 4** — QA/QC calc (`lib/data/qaqc.ts` + tests). 17/17 pass.

Total tests verdes: **38/38** en libs nuevas.

## Pendiente inmediato — Task 5

**Firestore rules + indexes for multi-org RBAC.** Ver plan líneas 737-877.

Files:
- Modify: `firestore.rules` (repo root)
- Modify/create: `firestore.indexes.json` (repo root)

Resumen Task 5:
- Reescribir `firestore.rules` con paths `orgs/{orgId}/{faenas,members,...}` + role-based access.
- Composite indexes para queries de reconc/qaqc/sondajes filtradas por yacId + periodo.
- NO desplegar (`firebase deploy`) en sesión nueva — sólo preparar archivos. Despliegue cuando Pablo confirme.

## Bloqueos / decisiones abiertas

- **Seed CMSG (Task 7):** requiere UID real Héctor Figueroa + service-account.json. Pablo provee.
- **Datos demo reconc/qaqc (Task 15):** Pablo coordina con Héctor.
- **Despliegue Firestore rules:** confirmar antes de `firebase deploy --only firestore:rules,firestore:indexes`.

## Stack confirmado

- Next.js 15 App Router · React 19 · TS · Firebase 11 · Tailwind · Vitest
- Vercel: `geoagent-app.vercel.app` (`.vercel/` solo en repo root)
- recharts ya instalado `^2.15.4`
- Lockfile real en `web/package-lock.json` (npm workspaces) — NO `web/apps/web/package-lock.json`

## Reglas operativas vivas (CLAUDE.md §2)

1. Bitácora `bitacora.md` actualizar al cierre.
2. Prompt log `prompts/YYYY-MM-DD/HHMMSS-<slug>.md` por prompt no trivial.
3. Handoff cuando contexto >60-70%.
4. Ciclo cierre: type-check + tests + commit + push + deploy + bitácora.
5. Modo subagent-driven con review checkpoint entre tasks.

## Prompt copy-paste para nueva sesión

```
Continúo trabajando en GeoAgent-App. Lee primero (en este orden):

1. CLAUDE.md  — instrucciones maestras del proyecto (reglas operativas §2 obligatorias)
2. tasks/handoff-2026-05-04-1554.md  — estado al cierre (4/15 tasks completas)
3. docs/superpowers/plans/2026-05-04-geology-super-cockpit.md  — plan F0+F1, líneas 737+ para Task 5

Estoy listo para continuar desde Task 5 (Firestore rules + indexes multi-org RBAC).

Ejecutá en modo subagent-driven (un subagente por task, review checkpoint entre tasks).
Antes de tocar código, registrá este prompt en `prompts/2026-05-04/HHMMSS-execute-plan-task-5.md` siguiendo schema de `prompts/README.md`.

NO desplegar firebase rules en esta sesión — sólo preparar archivos.

Caveman mode ultra activado para chat (código y commits normales). go
```
