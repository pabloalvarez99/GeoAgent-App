# Handoff — 2026-05-04 22:30

## Estado actual

Sesión de brainstorm + planning completa. **Cero código de implementación escrito todavía.** Próxima sesión arranca ejecutando Task 1 del plan.

## Plan / spec activos

- **Spec:** `docs/superpowers/specs/2026-05-04-geology-super-cockpit-design.md`
- **Plan:** `docs/superpowers/plans/2026-05-04-geology-super-cockpit.md` (15 tasks TDD)

## Trabajo entregado en esta sesión

| Commit | Mensaje |
|---|---|
| `e98cc67` | docs(spec): geology superintendent cockpit F0+F1 design |
| `a72dbdd` | docs(plan): F0+F1 implementation plan 15 tasks TDD-style |
| `b103dab` | docs(claude): handoff protocol cuando contexto >70% para ahorrar tokens |
| `e72ff08` | docs(claude): rewrite profesional CLAUDE.md + sistema de prompt log auditable |

**Archivos clave creados/modificados:**

- `CLAUDE.md` — rewrite profesional con TOC numerado + sistema de prompt log integrado
- `docs/superpowers/specs/2026-05-04-geology-super-cockpit-design.md`
- `docs/superpowers/plans/2026-05-04-geology-super-cockpit.md`
- `prompts/README.md`, `prompts/INDEX.md`
- `prompts/2026-05-04/_index.md` + 3 archivos retroactivos del día

## Pendiente inmediato

**Task 1 del plan** — Setup: instalar `recharts` + crear types `org.ts` y `reconc.ts`.

Comando exacto para arrancar:

```bash
cd web/apps/web
npm install recharts@^2.13.0
```

Luego crear `web/apps/web/src/types/org.ts` y `web/apps/web/src/types/reconc.ts` con el contenido detallado en Task 1 Step 2-3 del plan.

## Bloqueos / decisiones abiertas

- **Modo de ejecución del plan:** subagent-driven vs inline. No decidido. Sugerencia: subagent-driven para que cada task corra en contexto fresco (matchea protocolo handoff).
- **Seed CMSG (Task 7):** requiere UID real de Héctor Figueroa en Firebase Auth + service-account.json. Pablo debe proveer cuando esté listo.
- **Datos demo reconc/qaqc:** necesarios para demo final (Task 15 success criteria). Pablo debe coordinar con Héctor.

## Stack confirmado

- Next.js 15 App Router · React 19 · TypeScript · Firebase 11 · Tailwind · Vitest
- Vercel deploy: `geoagent-app.vercel.app` (linkeado vía `.vercel/` en repo root, NO en subdirs)
- Path data legacy `users/{uid}/...` → migración a `orgs/{orgId}/faenas/{faenaId}/yacimientos/{yacId}/...`

## Reglas operativas vivas (CLAUDE.md §2)

1. Bitácora `bitacora.md` actualizada al cierre.
2. Prompt log `prompts/YYYY-MM-DD/HHMMSS-<slug>.md` por cada prompt no trivial.
3. Handoff cuando contexto >60-70% (este archivo es ejemplo).
4. Ciclo cierre: type-check + tests + commit + push + deploy + bitácora.

## Prompt copy-paste para nueva sesión

```
Continúo trabajando en GeoAgent-App. Lee primero (en este orden):

1. CLAUDE.md  — instrucciones maestras del proyecto (reglas operativas §2 obligatorias)
2. tasks/handoff-2026-05-04-2230.md  — estado al cierre de la sesión anterior
3. docs/superpowers/plans/2026-05-04-geology-super-cockpit.md  — plan F0+F1, 15 tasks TDD

Estoy listo para empezar a ejecutar el plan desde Task 1 (setup recharts + types).

Ejecutá en modo subagent-driven (un subagente por task con review entre tasks).
Antes de tocar código, registrá este prompt en `prompts/<fecha-hoy>/HHMMSS-execute-plan-task-1.md` siguiendo el schema de `prompts/README.md`.

Caveman mode ultra activado para chat (código y commits normales).
```
