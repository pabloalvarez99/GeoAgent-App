{
  "name": "geoagent-web",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "web": "turbo run dev --filter=@geoagent/web",
    "desktop": "turbo run dev --filter=@geoagent/desktop"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.4"
}
